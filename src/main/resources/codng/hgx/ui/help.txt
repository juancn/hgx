USAGE hgx [options]
    -b, --branch  <branch name>
            Filter displayed history by branch. If not specified, it will use the current branch
    -B, --branch-only
            Constrain output only to commit in the selected branch

